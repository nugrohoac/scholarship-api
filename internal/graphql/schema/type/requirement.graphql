type requirement {
    id: Int
    scholarshipId: Int
    type: String
    name: String
    value: String
}

enum requirementType {
    document
}

input inputRequirement {
    type: requirementType!
    name: String!
    value: String!
}