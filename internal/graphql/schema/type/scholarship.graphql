type scholarship {
    id: Int
    sponsorId: Int
    sponsor: user
    name: String
    amount: Int
    status: Int
    image: image
    awardee: Int
    currentApplicant: Int
    deadline: String
    eligibilityDescription: String
    subsidyDescription: String
    requirementDescriptions: [String]
    fundingStart: String
    fundingEnd: String
    requirements: [requirement]
}

type scholarshipFeed {
    cursor: String
    scholarships: [scholarship]
}