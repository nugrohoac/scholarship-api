input inputApplicantEligibility {
    requirementId: Int!
    value: Boolean!
}

input inputApplicantScore {
    name: scoreName!
    value: Int!
}

enum scoreName {
    acceptance
    essay
    supporting_document
    total
}